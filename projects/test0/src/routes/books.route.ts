import { } from "../controllers/books.controller";
import { checkQuery, verifyToken, checkBody, acceptedBody } from "../utilities";

let bookType: BookModel = {
bookID: '',
title: '',
}

export default []
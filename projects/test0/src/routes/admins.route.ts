import { } from "../controllers/admins.controller";
import { checkQuery, verifyToken, checkBody, acceptedBody } from "../utilities";

let adminType: AdminModel = {
adminID: '',
name: '',
password: '',
}

export default []
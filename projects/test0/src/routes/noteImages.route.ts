import { } from "../controllers/noteImages.controller";
import { checkQuery, verifyToken, checkBody, acceptedBody } from "../utilities";

let noteImageType: NoteImageModel = {
userID: '',
noteID: '',
noteImageID: '',
image: 'image',
}

export default []